import React from 'react';

import './button.scss';

export default function Button() {
  return (
    <div>
      <button onClick={ this.Clicked("") }>Entrar</button>
    </div>
    <button onClick={this.}
  )
}
